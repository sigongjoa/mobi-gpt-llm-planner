import React from 'react';

// This component is no longer used in the application.
// It returns null to effectively remove it from the UI.
const EmptyComponent: React.FC = () => {
  return null;
};

export default EmptyComponent;
